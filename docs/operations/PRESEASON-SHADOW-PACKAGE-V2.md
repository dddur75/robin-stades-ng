# Preseason Shadow Package V2

Le package versionne datasets, hashes, code, source marché, politique de prix,
gates, stratégies et règles de rejet. Il contient toujours :

`NO_BET_DEFAULT = true`, `REAL_BETS = false`,
`PRODUCTION_LOCKED = true`.

Sans edge externe validé, il reste un package de monitoring marché et
calibration, sans pari.
